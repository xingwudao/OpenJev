[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "openjev"
version = "0.1.0"
description = "Python client for the OpenJev decision API"
requires-python = ">=3.11"

[tool.setuptools.package-data]
openjev = ["py.typed"]
